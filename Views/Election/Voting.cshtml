@model InternetVotingApplication.ViewModels.KandydatViewModel
@{
    ViewData["Title"] = "Głosowanie";
}
<br />
<br />
<br />
<br />
<div class="container">
    <main id="main">
        <div class="text-center">
            <h2>Głosowanie</h2>
            <form asp-action="VotingAdd" method="post">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th scope="col">Numer kandydata</th>
                            <th scope="col">Imię</th>
                            <th scope="col">Nazwisko</th>
                            <th scope="col">Zaznacz kandydata</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var election in Model.ElectionCandidates)
                        {
                            <tr>
                                <td>@election.Id</td>
                                <td>@election.Imie</td>
                                <td>@election.Nazwisko</td>
                                <td><input type="checkbox" name="candidate" value="@election.Id" onclick="MutExChkList(this);" /></td>
                            </tr>
                        }
                    </tbody>
                </table>
                <div class="form-group text-center">
                    <input type="hidden" name="election" value="@ViewBag.ID" />
                    <input type="submit" value="Zagłosuj" class="btn btn-primary" />
                </div>
            </form>
        </div>
    </main>
</div>

<script type="text/javascript">
    function MutExChkList(chk) {
        var chkList = chk.parentNode.parentNode.parentNode;
        var chks = chkList.getElementsByTagName("input");
        for (var i = 0; i < chks.length; i++) {
            if (chks[i] != chk && chk.checked) {
                chks[i].checked = false;
            }
        }
    }
</script>
